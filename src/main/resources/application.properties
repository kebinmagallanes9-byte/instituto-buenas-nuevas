# Datos de conexión PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/instituto_biblico_buenas_nuevas
spring.datasource.username=postgres
spring.datasource.password=Kmml9178480.-

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Server
server.port=8080

# Deshabilitar inicialización de datos automática (opcional)
spring.sql.init.mode=never

# Permitir acceso desde otras dispositivos en la red
server.address=0.0.0.0